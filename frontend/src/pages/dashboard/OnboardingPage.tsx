import { useState } from 'react';
import { Link } from 'react-router-dom';
const steps = ['Primeiro profissional', 'Conectar WhatsApp', 'Convidar equipe'];
export default function OnboardingPage() { const [done, setDone] = useState<boolean[]>([false, false, false]); return <div><div className="page-header"><div><h1 className="page-title">Primeiros passos</h1><p className="page-subtitle">Você pode concluir estas etapas quando quiser.</p></div><Link className="btn btn-secondary" to="/agenda">Continuar depois</Link></div><div className="onboarding-list">{steps.map((step, index) => <label className="glass-card onboarding-step" key={step}><input type="checkbox" checked={done[index]} onChange={() => setDone((current) => current.map((value, position) => position === index ? !value : value))} /><span><strong>{index + 1}. {step}</strong><small>{index === 0 ? 'Cadastre o profissional e os horários de atendimento.' : index === 1 ? 'Use as credenciais fornecidas pela sua integração de WhatsApp.' : 'Envie convites para sua equipe.'}</small></span></label>)}</div></div>; }
